# ============================================================
# PetConnect 环境变量配置示例
# 复制此文件为 .env.local 并填写对应的值
# ============================================================

# Supabase 项目 URL（在 Supabase Dashboard → Settings → API 中获取）
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key（公开密钥，在 Supabase Dashboard → Settings → API 中获取）
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Google Gemini API 密钥（如需 AI 能力，在 https://aistudio.google.com/apikey 获取）
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================
# Supabase Storage 头像上传配置说明
# ============================================================
# 头像上传使用 Supabase Storage，无需额外配置密钥。
# 但需要在 Supabase Dashboard 中手动创建存储 Bucket：
#
# 1. 登录 https://supabase.com/dashboard
# 2. 进入你的项目 → Storage → Buckets
# 3. 点击 "New bucket"，名称填写：avatars
# 4. 勾选 "Public bucket"（允许公开读取头像）
# 5. 点击 "Create bucket" 完成创建
#
# 创建 Bucket 后，在 SQL Editor 中执行 supabase/storage_policies.sql
# 以配置：认证用户可上传到自己的文件夹、所有人可读取头像
# ============================================================
